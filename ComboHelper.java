// Unpacks combo meals recursively
public class ComboHelper {
  public static List<MenuItem> unpackCombo(MenuItem combo) {
    // If it is a combo, return each item in it recursively (when working on this)
  }
}
